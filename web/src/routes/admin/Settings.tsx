import React from 'react';

export default function Settings(): JSX.Element {
  // Placeholder: echo-only config view in Phase 4
  return (
    <div>
      <h1>Admin Settings (Stub)</h1>
      <p>Echo-only. Persistence deferred.</p>
    </div>
  );
}
